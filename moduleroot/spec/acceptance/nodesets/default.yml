<% name = @configs['module_metadata']['name'].split('-').last rescue '[modulename]' %>
HOSTS:
  vagrant-<%= name %>01-local-ww00.wellspringworldwide.com:
    roles:
      - master
    platform: el-7-x86_64
    image: wellspring-beaker-centos7
    hypervisor: vagrant
    box: centos/7
CONFIG:
  log_level: verbose
  type: foss
